{{- if .Values.porter.ingress.enabled -}}
{{- $fullName := include "git-mirror.fullname" . -}}
{{- $svcPort := .Values.porter.service.port -}}
{{- if and .Values.porter.ingress.className (not (semverCompare ">=1.1.8-0" .Capabilities.KubeVersion.GitVersion)) }}
  {{- if not (hasKey .Values.porter.ingress.annotations "kubernetes.io/ingress.class") }}
  {{- $_ := set .Values.porter.ingress.annotations "kubernetes.io/ingress.class" .Values.porter.ingress.className }}
  {{- end }}
{{- end }}
{{- if semverCompare ">=1.19-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1
{{- else if semverCompare ">=1.14-0" .Capabilities.KubeVersion.GitVersion -}}
apiVersion: networking.k8s.io/v1beta1
{{- else -}}
apiVersion: extensions/v1beta1
{{- end }}
kind: Ingress
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "git-mirror.labels" . | nindent 4 }}
  {{- with .Values.porter.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if and .Values.porter.ingress.className (semverCompare ">=1.18-0" .Capabilities.KubeVersion.GitVersion) }}
  ingressClassName: {{ .Values.porter.ingress.className }}
  {{- end }}
  {{- if .Values.porter.ingress.tls }}
  tls:
    {{- range .Values.porter.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.porter.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- if and .pathType (semverCompare ">=1.18-0" $.Capabilities.KubeVersion.GitVersion) }}
            pathType: {{ .pathType }}
            {{- end }}
            backend:
              {{- if semverCompare ">=1.19-0" $.Capabilities.KubeVersion.GitVersion }}
              service:
                name: {{ $fullName }}
                port:
                  number: {{ $svcPort }}
              {{- else }}
              serviceName: {{ $fullName }}
              servicePort: {{ $svcPort }}
              {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
