"/var/log/nginx/access-{{getenv "MY_POD_NAME"}}.log" /var/log/nginx/error-{{getenv "MY_POD_NAME"}}.log {
  rotate 365
  daily
  postrotate
    kill -USR1 $(pgrep 'nginx: master')
  endscript
  nocompress
}
